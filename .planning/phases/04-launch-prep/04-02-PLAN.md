---
phase: 04-launch-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/index.ts
  - src/app/layout.tsx
autonomous: true
---

<objective>
Create homepage with hero section and footer with French content, completing the public-facing site structure.

Purpose: Transform placeholder homepage into a compelling landing page that drives visitors to browse products.
Output: Homepage hero, footer with legal links, complete site navigation structure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context
@.planning/phases/01-foundation-brand/01-02-SUMMARY.md

# Relevant source files
@src/app/page.tsx
@src/components/layout/Header.tsx
@src/components/ui/Button.tsx
@src/components/ui/Container.tsx
@src/lib/fonts.ts
@src/data/products.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create homepage hero section</name>
  <files>src/app/page.tsx</files>
  <action>
Replace placeholder homepage with a premium landing page:

1. Hero Section (full viewport height):
   - Background: bg-background with subtle gradient or solid
   - Large heading using font-heading: "L'art de la detente" (brand tagline)
   - Subheading: "Accessoires chicha premium pour les connaisseurs"
   - CTA button: "Decouvrir la collection" linking to /produits
   - Use Container size="lg" for content width
   - Center content vertically and horizontally
   - Typography: Use existing Cormorant Garamond for heading

2. Featured Products Section (optional if time):
   - Section title: "Selection Premium"
   - Grid of 3-4 featured products using ProductCard
   - Fetch featured products using getFeaturedProducts from products lib
   - Link to full catalog

Keep design minimal and elegant, matching brand aesthetic.
Import Button from components/ui for CTA.
  </action>
  <verify>Visit localhost:3000 - hero displays with French content and CTA links to /produits</verify>
  <done>Homepage shows hero section with brand tagline and navigation to products</done>
</task>

<task type="auto">
  <name>Task 2: Create site footer</name>
  <files>src/components/layout/Footer.tsx, src/components/layout/index.ts</files>
  <action>
Create a minimal, elegant footer component:

1. Footer.tsx:
   - Background: bg-primary with text-background (inverted from header)
   - Use Container size="lg" for content width
   - Three-column layout on desktop, stacked on mobile:

     Column 1 - Brand:
     - "Nuage" brand name
     - Tagline: "L'art de la detente"

     Column 2 - Navigation:
     - "Navigation" heading
     - Links: Accueil (/), Produits (/produits), Panier (/panier)

     Column 3 - Legal:
     - "Informations" heading
     - Links: Mentions legales (#), CGV (#), Contact (#)
     - Note: Use # for now, pages can be added later

   - Bottom bar with copyright:
     - "© 2026 Nuage. Tous droits reserves."

   - Padding: py-12 for main content, py-4 for copyright bar

2. Export Footer from components/layout/index.ts barrel file.
  </action>
  <verify>Import Footer in a test - TypeScript compiles without errors</verify>
  <done>Footer component created with brand, navigation, and legal links</done>
</task>

<task type="auto">
  <name>Task 3: Integrate footer into layout</name>
  <files>src/app/layout.tsx</files>
  <action>
Add Footer to the root layout:

1. Import Footer from @/components/layout
2. Add Footer after {children} inside CartProvider
3. Wrap children in a flex container to push footer to bottom:
   - Add wrapper div with className "flex flex-col min-h-screen"
   - Wrap children in main with className "flex-1"
   - Footer after main

Structure:
```
<CartProvider>
  <Header />
  <div className="flex flex-col min-h-screen">
    <main className="flex-1">{children}</main>
    <Footer />
  </div>
</CartProvider>
```

This ensures footer stays at bottom even on short pages.
  </action>
  <verify>Footer appears at bottom of every page, doesn't overlap content</verify>
  <done>Footer integrated into root layout, appears on all public pages</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] Homepage shows hero with French tagline and CTA
- [ ] CTA navigates to /produits
- [ ] Footer appears on all pages
- [ ] Footer has three columns on desktop
- [ ] Footer stacks properly on mobile
- [ ] Copyright shows current year
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Homepage is compelling and on-brand
- Footer provides navigation and legal structure
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-prep/04-02-SUMMARY.md`
</output>
