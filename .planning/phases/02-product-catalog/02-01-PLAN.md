---
phase: 02-product-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/product.ts, src/data/products.ts, src/app/produits/page.tsx, src/app/produits/[slug]/page.tsx, src/components/product/ProductCard.tsx, src/components/product/ProductGrid.tsx, src/components/product/index.ts]
autonomous: true
---

<objective>
Create product data model and public catalog pages with premium presentation.

Purpose: Establish the product type system and create the customer-facing product browsing experience.
Output: Working product listing and detail pages displaying sample products with brand styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-brand/01-03-SUMMARY.md

@src/app/globals.css
@src/components/ui/Button.tsx
@src/components/ui/Container.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create product type definitions and sample data</name>
  <files>src/types/product.ts, src/data/products.ts</files>
  <action>
Create TypeScript types for products in src/types/product.ts:
- Product: id, slug, name, description, shortDescription, price (number in cents), compareAtPrice (optional, for sale display), images (array of URLs), category, inStock, featured
- ProductCategory: enum with values 'chicha' | 'bol' | 'tuyau' | 'charbon' | 'accessoire'

Create sample product data in src/data/products.ts:
- Export array of 6-8 sample products across categories
- Use placeholder image URLs (via placeholder.co with brand colors)
- Prices in cents (e.g., 4999 for 49.99 EUR)
- Mix of featured and non-featured, some with compareAtPrice for sale items
- French product names and descriptions (e.g., "Chicha Crystal Premium", "Bol en Silicone Pro")
- Export helper functions: getProductBySlug(slug: string), getProductsByCategory(category: ProductCategory), getFeaturedProducts()
  </action>
  <verify>TypeScript compiles without errors: npx tsc --noEmit</verify>
  <done>Product types defined, 6-8 sample products available, helper functions work</done>
</task>

<task type="auto">
  <name>Task 2: Create ProductCard and ProductGrid components</name>
  <files>src/components/product/ProductCard.tsx, src/components/product/ProductGrid.tsx, src/components/product/index.ts</files>
  <action>
Create ProductCard component (src/components/product/ProductCard.tsx):
- Props: product (Product type), priority (boolean for image loading)
- Display: Product image (16:9 aspect ratio), name, price, "Voir" button
- If compareAtPrice exists, show crossed-out original price with sale price
- Use brand typography: name in heading font, price prominent
- Hover effect: subtle scale transform on image, accent color on button
- Link entire card to /produits/[slug]
- Use next/image with placeholder blur

Create ProductGrid component (src/components/product/ProductGrid.tsx):
- Props: products (Product[]), columns (2 | 3 | 4, default 3)
- Responsive grid: 1 col mobile, 2 col tablet, columns prop on desktop
- Gap using brand spacing tokens

Create barrel export in src/components/product/index.ts
  </action>
  <verify>Components render without errors in isolation</verify>
  <done>ProductCard shows product with styling, ProductGrid arranges cards responsively</done>
</task>

<task type="auto">
  <name>Task 3: Create product listing page</name>
  <files>src/app/produits/page.tsx</files>
  <action>
Create product catalog page at /produits:
- Page title: "Nos Produits" in heading font
- Use Container component (lg size)
- Category filter buttons at top (All, Chichas, Bols, Tuyaux, Charbon, Accessoires)
- Use URL search params for category filtering (?categorie=chicha)
- ProductGrid displaying filtered products (or all if no filter)
- Empty state if category has no products: "Aucun produit dans cette catégorie"
- Page metadata: title "Produits | Nuage", description for SEO

Style filter buttons:
- Horizontal scroll on mobile, wrap on desktop
- Active category uses accent background
- Inactive uses secondary button style
  </action>
  <verify>npm run build succeeds, page renders at /produits</verify>
  <done>Catalog page shows products, category filtering works via URL params</done>
</task>

<task type="auto">
  <name>Task 4: Create product detail page</name>
  <files>src/app/produits/[slug]/page.tsx</files>
  <action>
Create dynamic product detail page at /produits/[slug]:
- Fetch product by slug from data, return notFound() if not found
- Two-column layout on desktop: image left (sticky), details right
- Single column on mobile: image top, details below

Image section:
- Main product image (square aspect ratio)
- Image gallery thumbnails if multiple images (horizontal scroll)

Details section:
- Product name (h1, heading font, large)
- Price display (prominent, if sale show both prices)
- Short description (body font, muted color)
- "Ajouter au panier" button (primary, large) - disabled for now, will work in Phase 3
- "Stock disponible" or "Rupture de stock" indicator
- Full description below in prose style

Generate static params for all product slugs (generateStaticParams)
Generate metadata per product (generateMetadata)
  </action>
  <verify>npm run build succeeds, detail pages render for each product slug</verify>
  <done>Product detail page shows all product info with premium layout</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] /produits page displays all sample products
- [ ] Category filtering works via URL params
- [ ] /produits/[slug] pages render for each product
- [ ] No TypeScript errors
- [ ] Styling matches brand identity (colors, typography, spacing)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Products display with premium presentation
- Navigation between listing and detail pages works
- Responsive design works on mobile/desktop
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-catalog/02-01-SUMMARY.md`
</output>
