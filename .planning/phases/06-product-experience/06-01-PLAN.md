---
phase: 06-product-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/produits/[slug]/page.tsx, src/components/product/ProductCard.tsx]
autonomous: false
---

<objective>
Transform product pages with interactive galleries, image zoom, and enhanced hover effects.

Purpose: Elevate product presentation from static to dynamic, allowing customers to explore products in detail and creating a premium browsing experience.
Output: Interactive thumbnail gallery with zoom capability on product detail pages, enhanced ProductCard hover effects.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-motion-foundation/05-01-SUMMARY.md

# Framer Motion foundation available from Phase 5
# Motion patterns established: viewport intersection, staggered animations, 300-600ms durations

@src/app/produits/[slug]/page.tsx
@src/components/product/ProductCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make thumbnail gallery interactive</name>
  <files>src/app/produits/[slug]/page.tsx</files>
  <action>
Convert product detail page to client component. Add state for selected image index (default 0). Wire thumbnail buttons to change main image on click with smooth fade transition using Framer Motion AnimatePresence. Add active state styling to selected thumbnail. Preserve existing sticky positioning and responsive layout. Use key prop on main image for smooth transitions between images.
  </action>
  <verify>npm run build succeeds, visit /produits/[any-product], click thumbnails and confirm main image changes smoothly</verify>
  <done>Clicking thumbnails changes main image with fade transition, active thumbnail has accent border</done>
</task>

<task type="auto">
  <name>Task 2: Add image zoom on product detail page</name>
  <files>src/app/produits/[slug]/page.tsx</files>
  <action>
Add click handler to main product image that opens fullscreen zoom modal. Create modal with dark backdrop (bg-primary/90), centered image, close button (X icon top-right), click-outside-to-close. Use Framer Motion for modal entrance (scale from 0.95, opacity 0→1, 300ms). Support ESC key to close. Include left/right arrow navigation if multiple images. Lock body scroll when modal open. Use Next.js Image with fill for responsive sizing in modal.
  </action>
  <verify>Click main product image opens zoom modal, ESC/click-outside closes, arrows navigate images (if multiple), body scroll locked during modal</verify>
  <done>Image zoom modal works with keyboard and click controls, smooth entrance animation, accessible close button</done>
</task>

<task type="auto">
  <name>Task 3: Enhanced ProductCard hover effects</name>
  <files>src/components/product/ProductCard.tsx</files>
  <action>
Add motion.div wrapper with whileHover for card lift effect (y: -8px, shadow increase). Enhance existing image scale on hover (1.05 → 1.1 for more pronounced effect). Add Framer Motion to "Voir" button with whileHover scale effect (scale: 1.05). Add motion to price with subtle color shift on card hover. Keep existing scroll-triggered entrance animation. All transitions should be smooth (300ms duration). Do NOT add hover effects to "Ajouter au panier" button to avoid conflicting with its primary CTA status.
  </action>
  <verify>npm run build succeeds, hover over product cards shows lift effect, image zooms more, "Voir" button scales</verify>
  <done>ProductCard responds to hover with card lift, enhanced image zoom, button scale effects - all smooth and premium-feeling</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interactive product galleries with zoom and enhanced card hover effects</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/produits
    3. Hover over product cards: Verify card lifts, image zooms noticeably, "Voir" button scales
    4. Click any product card to visit detail page
    5. Click thumbnails: Verify main image changes smoothly with fade transition
    6. Click main product image: Verify zoom modal opens with dark backdrop
    7. Test modal: ESC key closes, click outside closes, arrow keys navigate (if multiple images)
    8. Check mobile (375px): Verify interactions work on touch, no layout issues
    9. Confirm: All animations feel smooth and premium (not jarring)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] Thumbnail gallery is interactive with smooth transitions
- [ ] Image zoom modal works with all controls
- [ ] ProductCard hover effects enhance without overwhelming
- [ ] All interactions work on mobile/touch devices
- [ ] No console errors or layout issues
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- User approved visual and interaction quality
- No errors or warnings introduced
- Product experience feels premium and polished
  </success_criteria>

<output>
After completion, create `.planning/phases/06-product-experience/06-01-SUMMARY.md`
</output>
