---
phase: 01-foundation-brand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, tailwind.config.ts, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, .gitignore, .env.example]
autonomous: true
---

<objective>
Scaffold Next.js 14+ project with App Router, TypeScript, and Tailwind CSS configured for a premium e-commerce store.

Purpose: Establish the technical foundation before brand identity and design system work.
Output: Working Next.js project with modern configuration ready for development.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with TypeScript and Tailwind</name>
  <files>package.json, tsconfig.json, tailwind.config.ts, next.config.ts, postcss.config.mjs</files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack` in the project root.

    If prompted about existing files, allow overwrite (only .planning and .git exist).

    After scaffolding:
    - Verify package.json has next, react, react-dom, typescript, tailwindcss
    - Verify tsconfig.json has proper path aliases configured
    - Verify tailwind.config.ts exists with content paths
  </action>
  <verify>npm run build succeeds without errors</verify>
  <done>Next.js 14+ project scaffolded with TypeScript, Tailwind, ESLint, App Router, src directory</done>
</task>

<task type="auto">
  <name>Task 2: Configure base layout and clean starter content</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, .gitignore</files>
  <action>
    1. Update src/app/layout.tsx:
       - Set lang="fr" for French market
       - Add metadata: title "Chicha Premium" (placeholder), description about premium hookah accessories
       - Keep Inter font or similar clean sans-serif

    2. Clean src/app/page.tsx:
       - Remove all Next.js boilerplate content
       - Add simple placeholder: centered "Coming Soon" with minimal styling
       - No images or complex layouts yet

    3. Clean src/app/globals.css:
       - Keep only Tailwind directives (@tailwind base, components, utilities)
       - Remove any boilerplate CSS

    4. Update .gitignore:
       - Ensure node_modules, .next, .env*.local are ignored
       - Add .env (but keep .env.example)

    5. Create .env.example with placeholder structure (empty values, commented sections for future: database, payments, etc.)
  </action>
  <verify>npm run dev starts without errors, localhost:3000 shows "Coming Soon"</verify>
  <done>Clean base layout configured for French market, no boilerplate content, ready for brand/design work</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` starts dev server
- [ ] localhost:3000 shows clean placeholder page
- [ ] TypeScript compilation passes
- [ ] No ESLint errors
</verification>

<success_criteria>

- Next.js 14+ with App Router scaffolded
- TypeScript configured with path aliases
- Tailwind CSS configured and working
- Clean starter page (no boilerplate)
- French locale set in layout
- Git ignoring appropriate files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-brand/01-01-SUMMARY.md`
</output>
