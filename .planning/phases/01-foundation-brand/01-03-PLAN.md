---
phase: 01-foundation-brand
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [tailwind.config.ts, src/app/globals.css, src/app/layout.tsx, src/lib/fonts.ts, src/components/ui/Button.tsx, src/components/ui/Container.tsx, src/components/ui/index.ts]
autonomous: true
---

<objective>
Implement design system foundation: Tailwind theme with brand colors/typography, base component library.

Purpose: Translate brand identity decisions into code for consistent implementation.
Output: Configured Tailwind theme and foundational UI components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/BRAND.md
@.planning/phases/01-foundation-brand/01-01-SUMMARY.md
@.planning/phases/01-foundation-brand/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Tailwind theme with brand tokens</name>
  <files>tailwind.config.ts, src/app/globals.css</files>
  <action>
    Read BRAND.md and implement the color palette and typography in Tailwind config.

    1. Update tailwind.config.ts:
       - Add brand colors from BRAND.md to theme.extend.colors
       - Use semantic names: primary, accent, background, text, muted, success, error
       - Configure font families for heading and body from BRAND.md choices
       - Add custom spacing if needed for generous whitespace

    2. Update src/app/globals.css:
       - Add CSS custom properties (--color-primary, etc.) for easy reference
       - Set base background color on body
       - Set base text color
       - Add any global resets (box-sizing, etc.)

    Structure colors like:
    ```typescript
    colors: {
      primary: { DEFAULT: '#...', light: '#...', dark: '#...' },
      accent: '#...',
      background: '#...',
      // etc.
    }
    ```
  </action>
  <verify>npm run build succeeds, Tailwind classes like bg-primary, text-accent work</verify>
  <done>Tailwind theme configured with all brand colors and fonts from BRAND.md</done>
</task>

<task type="auto">
  <name>Task 2: Set up fonts and update layout</name>
  <files>src/lib/fonts.ts, src/app/layout.tsx</files>
  <action>
    1. Create src/lib/fonts.ts:
       - Import fonts using next/font/google based on BRAND.md typography choices
       - Export font variables for use in layout
       - Configure appropriate weights (400, 500, 600, 700 as needed)

    2. Update src/app/layout.tsx:
       - Apply font variables to html/body
       - Update metadata with selected brand name
       - Ensure proper font fallbacks

    Example pattern:
    ```typescript
    // src/lib/fonts.ts
    import { Inter, Playfair_Display } from 'next/font/google'

    export const bodyFont = Inter({
      subsets: ['latin'],
      variable: '--font-body',
    })

    export const headingFont = Playfair_Display({
      subsets: ['latin'],
      variable: '--font-heading',
    })
    ```
  </action>
  <verify>Fonts load correctly on localhost:3000, no FOUT (flash of unstyled text)</verify>
  <done>Fonts configured and applied to layout, brand name in metadata</done>
</task>

<task type="auto">
  <name>Task 3: Create foundational UI components</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Container.tsx, src/components/ui/index.ts</files>
  <action>
    Create minimal base components following the brand style:

    1. src/components/ui/Button.tsx:
       - Primary variant (solid, brand color)
       - Secondary variant (outline)
       - Size variants (sm, md, lg)
       - Use semantic button element
       - Support asChild pattern for links (using Slot from @radix-ui/react-slot or simple implementation)
       - Consistent padding, border-radius per brand style

    2. src/components/ui/Container.tsx:
       - Max-width wrapper for content
       - Responsive padding
       - Centered content

    3. src/components/ui/index.ts:
       - Export all components for easy imports

    Keep components minimal - just enough for consistent baseline. More can be added as needed.

    Style approach: Use Tailwind classes, support className prop for customization.
  </action>
  <verify>Components render correctly, TypeScript types are valid</verify>
  <done>Button and Container components created with brand styling, exported from index</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Brand colors accessible via Tailwind classes
- [ ] Fonts load correctly without FOUT
- [ ] Button component renders with correct styling
- [ ] Container component constrains content properly
- [ ] No TypeScript errors
</verification>

<success_criteria>

- Tailwind theme matches BRAND.md specifications
- Fonts configured with next/font
- Button component with primary/secondary variants
- Container component for layout
- All brand tokens accessible in code
- Foundation ready for product catalog phase
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-brand/01-03-SUMMARY.md`
</output>
