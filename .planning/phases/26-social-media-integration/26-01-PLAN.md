---
phase: 26-social-media-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/social/SocialShareButtons.tsx, src/app/produits/[slug]/page.tsx, src/app/blog/[slug]/page.tsx, src/lib/seo.ts, src/lib/analytics.ts]
autonomous: true
---

<objective>
Implement social share buttons for products and blog posts to drive organic reach through social platforms.

Purpose: Enable easy content sharing to increase viral distribution and organic traffic. Each share extends brand reach and drives referral traffic.
Output: Functional share buttons on product and blog pages with share event tracking.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-seo-organic-growth/24-02-SUMMARY.md

Existing infrastructure:
@src/lib/seo.ts - OpenGraph and Twitter Card generators already exist
@src/lib/analytics.ts - Analytics event tracking system
@src/app/produits/[slug]/page.tsx - Product detail page
@src/app/blog/[slug]/page.tsx - Blog post page
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SocialShareButtons component</name>
  <files>src/components/social/SocialShareButtons.tsx</files>
  <action>Create reusable SocialShareButtons component that renders share buttons for Facebook, Twitter (X), WhatsApp, LinkedIn, and Copy Link. Use native Web Share API when available (mobile), fallback to window.open for desktop. Props: url (required), title (required), description (optional), variant ('floating' | 'inline'). Design: Icon-only buttons with tooltips, accent color on hover, responsive sizing. Include share success feedback (toast/check icon). Track share events via trackEvent('share', {platform, url, title}). Use lucide-react icons. Avoid third-party share SDKs (privacy concerns, page weight).</action>
  <verify>Component renders with all share buttons, click triggers share/window.open, events tracked</verify>
  <done>SocialShareButtons component exported and functional</done>
</task>

<task type="auto">
  <name>Task 2: Integrate share buttons on product and blog pages</name>
  <files>src/app/produits/[slug]/page.tsx, src/app/blog/[slug]/page.tsx</files>
  <action>Add SocialShareButtons to product detail pages (after product description, before related products) and blog post pages (after content, before comments/related posts). For products: pass full URL, product name as title, shortDescription. For blog: pass full URL, article title, excerpt. Use 'inline' variant on desktop, 'floating' on mobile (<768px). Ensure proper URLs with canonical domain (https://nuage.fr/produits/... or /blog/...). Test share preview by checking OG tags are present.</action>
  <verify>Share buttons visible on product and blog pages, open correct share dialogs with pre-filled content</verify>
  <done>Share functionality works on both product and blog pages with proper metadata</done>
</task>

<task type="auto">
  <name>Task 3: Enhance Open Graph images for better social previews</name>
  <files>src/lib/seo.ts, src/app/produits/[slug]/page.tsx</files>
  <action>Update generateOpenGraphTags and product page metadata to include optimized OG images. For products: use product.images[0] as og:image, ensure dimensions 1200x630px (add og:image:width and og:image:height). Add og:image:type. For blog: use featured image or default brand image. Ensure images are absolute URLs (https://nuage.fr/...). Update product page generateMetadata to include openGraph.images array with proper dimensions. Test with Facebook Sharing Debugger (https://developers.facebook.com/tools/debug/) and Twitter Card Validator.</action>
  <verify>OG image tags present with dimensions, absolute URLs, sharable preview looks correct</verify>
  <done>Social share previews show rich cards with images, title, description</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Share buttons render on product pages
- [ ] Share buttons render on blog pages
- [ ] Web Share API works on mobile
- [ ] Share dialogs open with correct content on desktop
- [ ] Share events tracked in analytics
- [ ] OG tags include image dimensions
- [ ] Social previews look professional (test with Facebook/Twitter debuggers)
</verification>

<success_criteria>

- All tasks completed
- SocialShareButtons component reusable and well-designed
- Share buttons integrated on product and blog pages
- Share events tracked for analytics
- Open Graph images optimized for social platforms
- No TypeScript errors
- No new console warnings
  </success_criteria>

<output>
After completion, create `.planning/phases/26-social-media-integration/26-01-SUMMARY.md`
</output>
